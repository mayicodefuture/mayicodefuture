import { Header, Container } from "components/layout";

const Blog = () => {
  return (
    <section id="blog">
      <Header />
      <Container>Blog</Container>
    </section>
  );
};

export default Blog;
