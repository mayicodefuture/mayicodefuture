import { Header, Container } from "components/layout";

const about = () => {
  return (
    <section id="about">
      <Header />
      <Container>About me</Container>
    </section>
  );
};

export default about;
